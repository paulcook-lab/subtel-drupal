apiVersion: v1
kind: Service
metadata:
  name: drupal
  namespace: subtel
  labels:
    app: drupal
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: drupal
  sessionAffinity: ClientIP
